{
  "scripts": {
    "install:all": "npm install && npm run install:react && npm run install:vue && npm run install:svelte && npm install three @threlte/core && npm install three @threlte/extras",
    "install:react": "cd react-app && npm install",
    "install:vue": "cd vue-app && npm install",
    "install:svelte": "cd svelte-app && npm install",
    "build": "npm run build:react && npm run build:vue && npm run build:svelte && echo 'Build completed successfully!'",
    "build:react": "cd react-app && npm run build",
    "build:vue": "cd vue-app && npm run build",
    "build:svelte": "cd svelte-app && npm run build",
    "dev": "concurrently \"npm run dev:react\" \"npm run dev:vue\" \"npm run dev:svelte\"",
    "dev:react": "cd react-app && npm run dev",
    "dev:vue": "cd vue-app && npm run dev",
    "dev:svelte": "cd svelte-app && npm run dev"
  },
  "devDependencies": {
    "@types/three": "^0.168.0",
    "concurrently": "^9.0.1"
  },
  "dependencies": {
    "@threlte/core": "^7.3.1",
    "@threlte/extras": "^8.11.5",
    "three": "^0.168.0"
  }
}
